<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hodlinfo</title>
    <style>
        body {
            background-color: black;
            color: #fff;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .navbar {
            background-color: black;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            position: fixed;
            top: 0;
            left: 0;
            width: 98%;
            z-index: 1000;
        }

        .navbar h1 {
            margin: 0;
            font-size: 44px;
            color: #44777D;
        }

        .buttons-container {
            display: flex;
            align-items: center;
            flex-grow: 1;
            justify-content: center;
        }

        .button {
            margin: 0 10px;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            cursor: pointer;
            background-color: rgb(41, 38, 38);
            color: white;
        }

        .connect-telegram {
            background-color: #0088cc;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            cursor: pointer;
        }

        /* Switch styles */
        .switch {
            position: relative;
            display: inline-block;
            width: 60px;
            height: 34px;
        }

        .switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #ccc;
            -webkit-transition: .4s;
            transition: .4s;
            border-radius: 34px;
            margin-left: 3px ;
        }

        .slider:before {
            position: absolute;
            content: "";
            height: 26px;
            width: 26px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            -webkit-transition: .4s;
            transition: .4s;
            border-radius: 50%;
        }

        input:checked + .slider {
            background-color: #2196F3;
        }

        input:focus + .slider {
            box-shadow: 0 0 1px #2196F3;
        }

        input:checked + .slider:before {
            -webkit-transform: translateX(26px);
            -ms-transform: translateX(26px);
            transform: translateX(26px);
        }

        /* Rounded sliders */
        .slider.round {
            border-radius: 34px;
        }

        .slider.round:before {
            border-radius: 50%;
        }

        .trade {
            
            text-align: center; /* Center the text */
            margin-top: 150px; /* Adjust margin-top as needed */
            font-size: 26px; /* Adjust font size */
            margin-right: 22px;
            font-weight: lighter;
        }
        .trade h2{
            color: #eed3d3;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 100px; /* Adjust margin-top as needed */
        }

        th, td {
            padding: 12px; /* Increase padding for better spacing */
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #221c1c;
            color: #fff; /* Adjust text color for better visibility */
        }
        .container {
    display: flex;
    flex-wrap: nowrap; /* Prevent wrapping */
    justify-content: space-between;
    align-items: center;
    padding: 20px;
    overflow-x: auto; /* Enable horizontal scrolling if content overflows */
}

.timePercentage {
    width: 30%;
    padding: 20px;
    border: 1px solid #ccc;
    border-radius: 10px;
    margin-right: 20px; /* Add margin between elements */
    text-align: center;
}

/* Adjustments to font sizes, colors, padding, and margins */
.percentage {
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 10px;
}

.timespam {
    font-size: 18px;
    color: #666;
}

h2 {
    font-size: 32px;
    color: #333;
}

h3 {
    font-size: 24px;
    margin-top: 20px;
}
.percentage {
    font-size: 44px;
    font-weight: bold;
    color: rgb(134, 134, 224); /* Set color to blue */
    margin-bottom: 10px;
}
    </style>
</head>
<body>
    <div class="navbar">
        <h1>Hodlinfo</h1>
        <div class="buttons-container">
            <button class="button btc">Buy BTC</button>
            <button class="button sell-btc">Sell BTC</button>
            <button class="button inr">INR</button>
        </div>
        <button class="connect-telegram">Connect Telegram</button>
        <label class="switch">
            <input type="checkbox">
            <span class="slider round"></span>
        </label>
    </div>

    <div class="trade">
        <h2>Best Price to Trade</h2>
    </div>
    <div class="container">
        <div class="timePercentage">
            <p class="percentage">0.5 %</p>
            <p class="timespam">5 Mins</p>
        </div>
        <div class="timePercentage">
            <p class="percentage">2.4% </p>
            <p class="timespam">1 Hour</p>
        </div>
        <div class="timePercentage">
            <h2>2255225</h2>
        </div>
        <div class="timePercentage">
            <p class="percentage">5.3% </p>
            <p class="timespam">1 Day</p>
        </div>
        <div class="timePercentage">
            <p class="percentage">7.4%</p>
            <p class="timespam">7 Days</p>
        </div>
    </div>
    <h3 style="margin-left: 600px;">Average BTC/INR set price including commission</h3>
    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Last Trade Price</th>
                <th>Buy/Sell Price Difference</th>
                <th>Difference</th>
                <th>Base Unit</th>
            </tr>
        </thead>
        <tbody><% data.forEach((ticker, index) => { %>
            <tr>
                <td><%= index + 1 %></td>
                <td><%= ticker.name %></td>
                <td><%= ticker.sell %></td>
                <td><%= ticker.buy %>/<%= ticker.sell %></td>
                <% 
                    // Calculate the percentage difference between buy and sell prices
                    const percentageDifference = ((ticker.buy - ticker.sell) / ticker.sell) * 100;
                    // Round the percentage difference to three decimal places
                    const roundedPercentageDifference = percentageDifference.toFixed(3);
        
                    // Apply different styles based on the sign of the percentage difference
                    const color = percentageDifference > 0 ? 'green' : 'red';
                %>
                <td style="color: <%= color %>"><%= roundedPercentageDifference %></td>
                <td><%= ticker.base_unit %></td>
            </tr>
        <% }); %>
        
        </tbody>
    </table>
</body>
</html>
